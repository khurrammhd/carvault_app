gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Exception: this is a dedicated DEBUG-only keystore (see build.gradle.kts),
# checked in on purpose so every build — local or CI — signs identically.
# It has no release/production authority and cannot publish anything.
!keystores/debug.keystore
